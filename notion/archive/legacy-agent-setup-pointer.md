# Notion Agent setup

This file is retained as a compatibility pointer for the earlier setup guide.

Use [`NOTION_COMPLETE_SETUP_PROMPT.md`](NOTION_COMPLETE_SETUP_PROMPT.md) as the canonical Notion setup instructions. It matches the current workflow and database fields, including:

- `Class` for the lowercase course code;
- `Week` values such as `W1`;
- `Anki Deck` values such as `comp307`;
- `Status = Ready` and `Status = Imported`;
- lecture and textbook summaries as equal flashcard sources;
- duplicate prevention and source labeling;
- the `Generate Flashcards` request workflow.
